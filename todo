

In the future, create a function to abstract the craziness of reduce()



Create a reducer which adds, edits and deletes recipes from state.recipes array

next, initiate reqeust to server.


{ name, ingredients }

CREATE :





EDIT :






DELETE :






const assign = (a, b) => Object.assign({}, a, b)
const get = attr => object => object[attr]
const getId = get('id')

const recipes = ( type, recipe ) =>
  

export default ( state, { type, recipe }) => assign( state, { recipes: recipes( type, recipe ) } )
